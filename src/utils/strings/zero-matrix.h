#ifndef UTILS_STRINGS_ZERO_MATRIX_H_INCLUDED
#define UTILS_STRINGS_ZERO_MATRIX_H_INCLUDED

#include <vector> // std::vector

std::vector<std::vector<int> >& zeroMatrix(std::vector<std::vector<int> > &);

#endif