#ifndef UTILS_STRINGS_STRING_ROTATION_H_INCLUDED
#define UTILS_STRINGS_STRING_ROTATION_H_INCLUDED

#include <string> // std::string

// check if str1 is a rotation of str2 using only one call to isSubstring
bool isStringRotation(const std::string &, const std::string &);
bool stringRotation(std::string, std::string);

#endif